# Optional, for `docker compose` on the command line: save as .env beside
# compose.yaml. Portainer users set these as stack environment variables instead.
# Nothing secret goes here: the init service generates the secrets.
PASSDOWN_URL=https://guides.example.org:8443
PASSDOWN_PORT=8443
